version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - CURRENCY=ARS
      - CACHE_DURATION=60
    volumes:
      - ./data:/app/data
    restart: unless-stopped
